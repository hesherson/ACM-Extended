# Local candidate AP: retain current successful-draw feedback

Test-only continuation after local AO. H361 retains its original pytest identity. Its old source assertion required an unnumbered Drawn! caption and a 0.45-second reset, while the current draw function deliberately uses Drawn! (count), followed by Draw (count) after 1.00 second. Existing B76 tests already protect the longer feedback interval.

The reconciled historical body verifies executable tokens for the successful-component count, its persisted count, the numbered captions, success color, one-second existing callback, and display/generation/preparation-mode guards. Comments cannot satisfy these checks.

Nineteen new checks pass against unchanged draw runtime. Six execute one, two and three successful pulls for repeated or alternating medications; four reject obsolete feedback after replacement/missing displays, stage changes or newer feedback generations; four verify that rejected draws do not count or schedule success; five mutations are rejected despite old-token comment decoys. The actual whole skCompoundDraw function executes with explicit stock, UI and scheduler fixtures.

No compound drawing, volume endpoint, save, font, UI timing, dose, inventory or asset behavior is changed. These are source-contract reconciliation and recorded UI requests, not pixel rendering or live inventory validation. General save/reopen lifecycle and duplicate input delivery are not certified.

The local candidate ledger moves 66 to 65. H264 was resolved in the retained prior AM-AN candidate. The published AK-AL ledger remains 67 until this cumulative candidate is independently validated and published. Every other H line remains unchanged. No test is skipped, removed, or marked xfail by this batch. HEMTT source check and local comparison outcomes are provided with the candidate evidence; incomplete assets prevent a complete-package or stable-release claim.
