# B157 validation

Base source: B156 `5ca185f`, tree `06c2911681328fa58e6006c36fb9aa7ebcb5f185`. A locally applied B156 mail patch can have a different commit ID while retaining this same source tree.

## Reproduced and covered paths

- First-contact weapon-preserving kneel, post-treatment generic crouch, actual supplied ACE animation dispatch, normal GUI close, missed Unload, rejected handoff, newer-owner protection, stale ACE end-animation hints, final speed restoration and patient switching.
- Semi-Fowler's to HTCL without torso suspension or roll, another provider maintaining manual support, same-provider occupied rejection before mutation, invalid callbacks without new input handlers, advanced-airway takeover and cancellation.
- Old head-position cleanup yielding to a newer action, including preserving that newer action's Direct Pressure exclusion. The two Direct Pressure failures were reproduced by reverting their fixes in memory, then passed with the corrected source.
- Check Breathing with and without a carrier, native timer starting only after the matching frozen hold and carrier-removal acknowledgement, success/failure release, failed native start, timeout, range/life/deletion cancellation, stale provider tokens and cancelled front-roll continuation.
- Debug shared text size, padded columns, numeric alignment and preservation of longer values. Font metrics are test fixtures; no Arma pixel-perfect rendering claim is made.
- Native animation-rate ownership. The B156 source reproduced a duration-ratio overwrite of the shared choreography rate; corrected source preserves the exact owned rate without changing the clinical timer or native short-action animation skip.

## Results

- Combined affected regression selection: **426 passed** in 87.28 seconds.
- Final airway/head handoff selection, including the two late Direct Pressure cases: **60 passed**. This overlaps the combined selection; do not add the counts.
- Source hygiene, native function registration, ACME symbol closure, public build identity, ACE override load order and named remote-exec target gates passed.
- `git diff --check` passed.

Tests execute checked-out SQF control flow using SQF-VM 2.0.0. Arma objects, displays, animation timing/appearance and transport are mocked engine boundaries. The supplied ACE animation dispatcher is executed for priority/interpolation checks. These tests are not an in-game dedicated-server run.

## Build and package verification

HEMTT 1.22.0 `release --no-bin --no-archive -v` completed successfully. Linux validation skips Windows model/animation binarization; deployment commands use normal Windows `hemtt release`.

- 14 PBOs, 14 matching signatures and one matching server key.
- 1,645 SQF source and 1,645 compiled SQFC entries.
- All **22 changed runtime SQF/HPP files** matched their packaged source bytes, with none missing or different.
- No Python cache/bytecode files in PBOs.
- The bundled command-wiki fallback and existing advisory lint suggestions did not prevent the build.

## Live validation boundary

Still verify in Arma with the actual dedicated-server modpack: interpolation appearance, another client's frozen-pose view, low-frame-rate entry, and debug text at the user's UI scale. Exercise first contact, procedure then menu close, patient switching, Semi-Fowler's to HTCL, and Check Breathing success/cancellation with and without a carrier.

Install the full fork on server and clients; keep original ACM disabled. The downloadable updater checks the B156 source tree rather than its locally rewritten commit hash, permits untracked release signatures, creates a unique local branch, applies B157 once, verifies the resulting tree and builds the complete release.
