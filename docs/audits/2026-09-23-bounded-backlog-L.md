# Bounded L: current click-only tag dropdown contracts

Builds on K from I-J parent `8ceec1ce08bd16af28064e3914f7277da010c6b1`. No runtime changes in this batch.

H257 now follows the pending selector's actual registration in `skPendingTagEnsure` and the stored selector's registration in `skInject`. Both retain None, LBSelChanged, valid-row MouseButtonUp fallback and row reset. H258 checks the current Select Syringe Tag / Edit Syringe Tag captions rather than retired abbreviations. H283 checks the shared canvas-width/below-selector layout rather than superseded sizing expressions. H338 checks the current click-only selector, dark list and artwork-before-selector-before-dropdown creation order; the obsolete hover-open expectation is not restored. All four original pytest identities remain.

26 new checks execute the actual click/fallback handler bodies or exact menu geometry block and mutation-check source contracts with comment decoys. They cover opening/closing, selection reset, missing displays, pending control exclusion from Body Map, stored editor entry, valid/negative rows, native-selection-plus-release delivery, shared menu geometry at representative canvas sizes, wrong callback targets, removed None, restored hover-open, retired captions and width drift. These pass on the unchanged dropdown runtime. Color-write and delayed focus behavior is independently covered by K's actual callbacks.

Controls, focus, list selection and event order are explicit fixtures. This does not simulate Arma event propagation, render pixels, establish font/text fitting, certify edge-case screen clipping or live mouse accessibility. The four failures were stale test contracts, not four gameplay fixes. No unreviewed test body, runtime, asset, configuration or protected snapshot is changed. The original index moves 119 to 115; all unreviewed H entries remain verbatim. No skip or xfail is added. No stable-release approval.

## Complete-checkout validation

Focused: {'passed': 313}. New cases on original I-J runtime: {'passed': 40, 'failed': 14}; failures reproduce the color-focus defect. Full addon before: {'passed': 3983, 'failed': 147, 'skipped': 4}; after: {'passed': 4041, 'failed': 143, 'skipped': 4}. Exactly four retained historical identities now pass and all 54 new cases pass. No prior identity is missing or newly failing. Both addon commands remain failing overall with the same four skips and zero collection/setup errors. Root before: {'error': 46, 'passed': 137}; after: {'error': 46, 'passed': 137}, with identical identities/outcomes. Full-project HEMTT check returns 0. Only the two documented color-focus runtime files change. All 4060 other existing tracked files retain their complete-checkout SHA256, including every other runtime, configuration, asset and protected snapshot. No live Arma or stable-release approval.
