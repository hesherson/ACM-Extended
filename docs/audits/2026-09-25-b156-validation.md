# B156 validation

Base: published B155 `0fc58f89b6db25df0a6586672e87c460c67a56a6`.

## Source contracts and reproduced defects

The supplied ACM deliberately replaces ACE's medication-count function with a scalar contract. This release preserves it, accepts ACE's current pair shape in Extended readers, and bridges the native medication summary. Tests execute the actual supplied ACE and fork medication implementations.

Executable regressions reproduced stale reset callbacks/workers, repeated medication Save, lost partial source volumes, obsolete syringe-size math, missing final partial-push time, incorrect shared inventory debits, and animation ownership races before correction. Tests retain valid progression, role checks, clinical timing and the existing treatment/custody protocols.

Shared inventory tests exercise patient-first, medic-first, medic-only and conditional-medic settings; patient-only and shared-vehicle stock; atomic multi-item rollback; original-donor refunds after dismount; duplicate receipts; native IO rejection; HPMK contention; ventilator battery ACK/cancel; deleted AI donor recovery; and actual NRB cylinder selection. Medication refunds queue on the source owner while another drawer holds a lease and settle against the current source ledger.

Animation tests exercise owner and observer freeze state, late/reordered packets, JIP before episode state, locality transfer, exact lease cancellation, sparse-frame chest preparation, menu unload versus newer displays, native continuous entry, ordinary ACE treatment handoff, and finite exit-speed release. Restored generic/prone continuous poses are recognized as empty-handed without starting another holster wait.

## Executed checks

All runtime commands set `PYTHONDONTWRITEBYTECODE=1` and `SQFVM` explicitly and use `python3 -B -m pytest -p no:cacheprovider`. SQF-VM is version 2.0.0, April 3, 2026.

| Run | Result |
| --- | --- |
| Medication, preparation, vial, infusion and protocol regression suite after owner-refund queue | 556 passed + 26 subtests |
| Affected medication cases after final Unload-source guard | 101 passed |
| Laryngoscopy, suction, procedure trays, IO/shared inventory and NA8.5 batch 2 | 178 passed |
| Coagulation, ACE summary, shared inventory/action gates, reset, compatibility and network/bootstrap integration | 139 passed |
| IV tray hover, geometry, PAA container, UI follow-up and build identity | 108 passed |
| Final combined integration selection | 585 passed; one obsolete source-text assertion corrected as described below |
| Final corrected role/held-tool assertion and executable tray regressions | 38 passed |

These runs overlap and must not be added together. The final integration selection contained 586 cases. Its sole failure expected denied chest-tube rows to disappear even while the user held that tool. The implemented fix retains the held tool only so it can be put down; clinical role requirements remain enforced. The source assertion was updated to that intended contract and rerun with executable tray tests. No runtime failure remained in that combined selection.

Source hygiene, native registration, ACME symbol closure, public version, ACE load order/target closure and remote-exec whitelist gates pass. Coverage: 389 native PREP registrations, 70 explicit native CfgFunctions sources, 1,149 Extended registrations, 70 frozen ACE override targets, 150 ACE macro targets, 78 literal ACE symbols and 22 named remote-exec targets. `git diff --check` passes.

## Coagulation measurements

The instrumented SQF execution observes no coagulation broadcasts for healthy candidates. A stable abnormal patient processed 100 ticks with only its initial five scalar publications. Active processing remains 5 Hz. Event/ownership/fluid hooks enroll immediately; other changes can use the existing registration scan or the 2-second fallback. Exact neutral recovery and owner-cache invalidation are tested. These are function-level publication counts, not measured server FPS or real network bandwidth.

## Release package

HEMTT 1.22.0 `release --no-bin --no-archive -v` completed successfully:

- 1,645 SQF files compiled, 16 configs rapified, 12 stringtables checked.
- 14 PBOs, 14 matching signatures, one `ACM_1.2.3.0.bikey`.
- All 160 modified/new runtime SQF/HPP sources compared byte-for-byte with their packaged entries; none missing or different.
- No Python bytecode/cache directories in the PBOs.

The bundled command wiki fallback and existing advisory lint suggestions did not prevent the build. Linux validation skips Windows model/animation binarization. Use ordinary `hemtt release` for deployment.

## Runtime boundary and deployment

No Arma renderer or dedicated server ran here. Actual modpack binding order, visual smoothness, native inventory/network delivery and low-tier-client performance still require the target server. Recheck two-client observer freezes, patient-only supplies, concurrent providers, cancel/reopen, full heal and ownership changes.

Deploy the complete fork to clients and server, with matching signatures/server key, and keep the separate original ACM disabled. Verify **1.2.3 / B156** and `[ACME COMPAT] OK` in a fresh RPT. The earlier supplied mixed-install RPT's external-mod errors remain documented in the B155 RPT audit.

Primary boundary reference: [CBA ownerEvent](https://cbateam.github.io/CBA_A3/docs/files/events/fnc_ownerEvent-sqf.html) targets a captured client ID, allowing inventory receipts to settle on their issuer after the medic's object changes owner.
