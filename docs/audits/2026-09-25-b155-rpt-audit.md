# B155: supplied screenshot and RPT audit

## Evidence and limits

Reviewed `image(5).png` and all 66,316 lines of
`Arma3_x64_2026-09-24_14-10-38(2).rpt`. Line references below refer to that
unchanged input. The screenshot shows the same 14-function compatibility warning
recorded at RPT lines 14255 and 15218. The raw log is not copied into this repository.

This is a client log from an older mixed installation, not a B154/B155 runtime
verification:

- Lines 2502–2513 mount all 12 base ACM PBOs from the separate **Advanced Combat
  Medicine** mod, version **1.4.8.0**.
- Lines 2562–2563 mount only `ACM_acm_extended.pbo` and `ACM_itemtext.pbo` from
  **ACM Extended**, version **1.2.2.1**.
- The launch command at line 3 enables both mods. The compatibility messages have
  the older format and include the since-retired `dropObject_carry` requirement.
- Line 15015 identifies the multiplayer segment as a player client joining in
  progress. The server's actual loaded files are not established by this client log.

Installing the full current fork on every client and the server, disabling the
separate upstream ACM mod, and restarting Arma is required to resolve this exact
mixed installation. Copying only the Extended/itemtext PBOs leaves the missing
fork implementations unchanged. Current source and offline tests cannot establish
which files a remote server will load next.

## Resolution of the 14 reported compatibility entries

All 13 retained requirements have executable version markers in the current source.
They are still checked at runtime. The test
`test_runtime_compatibility_install.py` rejects upstream, missing, and wrong-type
bindings and checks that the checker never replaces bindings or hides failures.

| Reported runtime function | Current source | Marker |
| --- | --- | --- |
| `ACM_circulation_fnc_getBloodVolumeChange` | `addons/circulation/functions/fnc_getBloodVolumeChange.sqf` | `B106:volumeCanonical` |
| `ace_medical_vitals_fnc_handleUnitVitals` | `addons/core/overrides/fnc_handleUnitVitals.sqf` | `B106:vasoconstrictionPersist` |
| `ACM_circulation_fnc_setIV` | `addons/circulation/functions/fnc_setIV.sqf` | `B106:setIVReconciled` |
| `ACM_airway_fnc_handleAirway` | `addons/airway/functions/fnc_handleAirway.sqf` | `B106:airwayWakeGuard` |
| `ACM_airway_fnc_handleAirwayCollapse` | `addons/airway/functions/fnc_handleAirwayCollapse.sqf` | `B125:airwayCollapseWakeClear` |
| `ACM_core_fnc_getUpPrompt` | `addons/core/functions/fnc_getUpPrompt.sqf` | `B106:getUpLifecycle` |
| `ACM_core_fnc_addVehiclePatientActions` | `addons/core/functions/fnc_addVehiclePatientActions.sqf` | `B106:vehicleUnloadGuard` |
| `ACM_disability_fnc_handleFracture` | `addons/disability/functions/fnc_handleFracture.sqf` | `B106:fracturePainChance` |
| `ACM_damage_fnc_wrapBodyPartLocal` | `addons/damage/functions/fnc_wrapBodyPartLocal.sqf` | `B106:wrappedWoundReopen` |
| `ace_dragging_fnc_canCarry` | `addons/core/overrides/fnc_canCarry.sqf` | `B106:ace321Carry` |
| `ace_dragging_fnc_canDrag` | `addons/core/overrides/fnc_canDrag.sqf` | `B106:ace321Drag` |
| `ace_interact_menu_fnc_compileMenuSelfAction` | `addons/core/overrides/fnc_compileMenuSelfAction.sqf` | `B106:ace321SelfMenu` |
| `ace_zeus_fnc_moduleUnconscious` | `addons/core/overrides/fnc_moduleUnconscious.sqf` | `B106:aiUnconsciousGuard` |
| `ace_dragging_fnc_dropObject_carry` | ACE owns this function; ACME uses `ace_dragging_stoppedCarry` in `fn_initForkStartupRuntime.sqf` | Requirement removed in B153; post-carry behavior retained through the event |

B155 also adds the missing `ace_zeus` dependency to `ACM_core`. This makes the
Zeus override's addon load order explicit. The override target manifest and its
load-order/target tests are updated to include the current unconscious and holster
targets and to remove the retired carry-drop target. These checks complement the
runtime markers; they do not prove runtime ownership in an arbitrary modpack.

## Confirmed ACME defects repaired in B155

| Evidence | Cause in current source | Repair |
| --- | --- | --- |
| Lines 57288–57289: missing `QEDaveMergens96` font | Both syringe-tag control classes selected the optional custom font during control creation. The later SQF fallback could not prevent that initial lookup. The font assets are absent from the repository. | Both control classes now start with the built-in `Caveat` font. Existing renderers still select the optional custom family after finding its `.fxy` file. |
| Lines 14183–14193 and 15198–15208: 22 attempts to override 11 final ACME expansion functions | `CfgFunctions` already registered the functions; `fn_expansionBootstrap.sqf` compiled and assigned them again unconditionally during each mission startup. | Removed the duplicate compilation loop. Preserved deferred runtime registration and its existing guard against duplicate handlers. |

Focused validation: **22 tests passed** across
`test_bounded_tag_font_fallback.py` and `test_expansion_bootstrap_runtime.py`, using
SQF-VM with no skipped runtime cases. Tests exercise absent/present optional font
selection, shared display cache, assignments to all tag controls, preservation of
compiled function bindings, deferred startup, and one set of expansion handlers
after repeated bootstrap calls. Arma font rendering and actual function compilation
remain engine boundaries.

## Deduplicated RPT script errors

The log contains **323** `Error in expression` blocks. The counts below partition
all 323, rather than counting their additional `Error position` lines again.
None names an ACME SQF function as its exception source.

| Count | Error family and first line | Current disposition |
| ---: | --- | --- |
| 203 | ITN `gjb_itn_modeSpec.sqf:174`, empty `gjb_itn_interface` array; line 15334 | External ITN/device configuration error; not fixed by ACME changes. |
| 2 | ITN `gjb_itn_getDeviceOffset.sqf:99/101`, undefined `_default`; lines 16111/16124 | External ITN error; requires its source/configuration. |
| 36 | CBA `fnc_addKeybind.sqf:122`, string supplied where numeric default key is expected; line 13936 | Log omits the registering caller, so attribution is unresolved. Both current ACME registrations use numeric defaults (`0xF2`, `0x21`). Do not label this a CBA implementation bug from this trace alone. |
| 6 | DEVGRU RS-EW cable monitor functions, missing semicolon; line 4068 | Three external files, each reported twice. |
| 6 | ISA_PIP `fn_playerInit.sqf:41`, string used as config entry for `OpticHasLrfMount`; line 15520 | External optic/LRF error. |
| 2 | Vehicle `HitEngine` expression undefined while compiling `hithull`; line 14470 | Vehicle configuration error, outside current ACME definitions. |
| 68 | Particle/cloudlet expressions: undefined surface/direction/density/humidity values and unmatched parentheses; line 19858 | Engine particle configuration errors. Current ACME cloudlet definitions do not edit the failing classes. Exact mod ownership is not established by these expression-only traces. |

Other substantial diagnostics, counted separately from those script errors:

| Count | Evidence | Disposition |
| ---: | --- | --- |
| 250 | Attempts to override final functions, starting line 13793 | Includes the 22 ACME expansion attempts repaired above, plus ACE and other mod functions. The remaining messages require a fresh log after deployment; a mixed installation prevents clean ownership attribution. |
| 506 + 120 | Missing `ISA_URGI_115` / `ISA_URGI_NGAL_WMX200` weapon classes, starting lines 14624/14671 | External equipment definitions or missing modpack dependencies; associated property/array warnings amplify the count. |
| 1,743 | Missing `zeppelin33` font sizes, starting line 18923 | This family is not referenced by the current ACME source. Separate from the repaired ACME tag font issue. |
| 386 | ACRE pipe `CreateFileA WinErrCode: 2`, starting line 15191 | ACRE integration/connection issue; not repaired by this patch. |
| 28 | ACE frag cannot create `ace_explosion_reflection_*` classes, starting line 65741 | ACE/configuration issue; current ACME source does not define or replace those classes. |
| 2 | ACM vitals synchronization warning: 10 seconds at line 15006, 8.23633 seconds at line 65577 | Indicates delayed synchronization in this older loaded session. Does not isolate network latency, scheduler pressure, or client hardware as the cause. |

Additional missing textures, weapon properties, skeleton bones, and remote objects
are present. They belong to named equipment/vehicle assets or lack sufficient
ownership detail in the log. They have not all been repaired by this ACME patch.

## Chest-seal interpretation

This RPT has no chest-seal entry exception or timestamped ACME preparation-stage
trace. It therefore cannot establish the exact animation failure or prove that
low client performance caused it. The mixed clinical installation is independently
established and needs correction. The B155 chest-entry changes and their tests
address observed source-level timing/handshake weaknesses separately; a fresh
dedicated-server test with the complete build remains necessary.

For that test, record a fresh startup log, confirm the current build stamp and
`[ACME COMPAT] OK`, then attempt chest-seal entry under the affected server load.
If the warning persists, its retained function names identify which live bindings
still differ. Do not suppress the checker to make the installation appear healthy.
