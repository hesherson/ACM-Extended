# Bounded BB: current tag-selector and edit-input reconciliation

BB reconciles the final four original source-contract identities:

- H269: ordinary Body Map Edit Syringe Tag geometry is owned by the same dynamic layout pass as the IV/IO + IM route row. The full-width edit row sits immediately below the route row and above the bounded carousel workspace.
- H282: Draw Syringe's Select Syringe Tag control is centered under the physical tag face using native syringe geometry. Medicated-flush tag metadata is attached at Save, not on every Draw.
- H284: Body Map's Edit Syringe Tag row uses the full route-row width; dedicated tag editing then switches to the stored syringe's own native geometry.
- H335: dedicated tag editing owns the keyboard/click surface. A/D navigation and center-toggle are intentionally blocked while edit mode is active, instead of moving the carousel underneath active text fields.

BB is test/audit-only. No runtime SQF, config, treatment logic, networking or assets change.

After BB the original historical H-ledger contains zero unresolved entries. This does **not** mean the complete addon suite has zero failures; newer/non-ledger test outcomes are tracked separately.
