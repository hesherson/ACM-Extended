# Bounded AN: current editor input masks

Builds on AM. H264 retains its original test identity. Its former assertion demanded one generic hitbox enabled solely by edit mode, predating the dedicated center hitbox and injection/carousel/size gates. The revised contract protects both actual center/neighbor masks, vascular/IM hotspot masks, and early editor rejection in Move/Pick. It does not restore the retired overlapping center input.

Twenty-seven new cases pass against unchanged presentation/navigation runtime. They evaluate the actual mask declarations for edit, injection, carousel and layout combinations, execute the complete Move/Pick functions while editing, and reject six gate/enabling mutations despite comment decoys. Expected outcomes are independent of the extracted expressions. Visibility/enabling call targets remain source-checked. No layout, font, navigation, animation, clinical or asset change in AN.

Only H264's historical body is reconciled. The ledger moves 67 to 66; all other historical entries remain verbatim. No tests are removed, skipped or xfailed. Numeric/control fixtures do not render a UI or certify live event propagation, screen clipping, fonts or entire display lifetimes. No live Arma or stable-release approval.
