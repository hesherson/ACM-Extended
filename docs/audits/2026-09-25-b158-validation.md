# B158 validation

Source base: B157 (`b78daf9` locally; GitHub `2e6cea5e3b75618ed34a47f5f47d918cc290e812`). Both base commits have tree `601056d1f2c2a2dfd285f9d0011f399b738e7841`.

## Automated checks

500 unique relevant cases passed across the validation runs. The initial combined selection passed 485 of 499; 14 older fixture failures were corrected and rerun successfully, and one additional treatment-rate regression was then added and passed. The final dedicated B158 file passes all 13 cases.

Coverage includes debug row alignment/continuations and measured layout; provider preparation and exact-once Putdown moves; head-position start, stop, death, locality, menu/movement cancellation, stale retry and owner handoffs; native CPR/BVM, chest-access restoration; B157 airway, menu, check-breathing, native-rate and provider-stop regressions; and release identity. SQF executes in SQF-VM with explicit engine-boundary fixtures.

The old head-start fixtures now acknowledge provider readiness instead of assuming every placement immediately moves the patient. The weapon fixture's expected delay uses the existing 1.5 choreography rate, which was already present in B157. The production weapon-preparation helper was not changed.

## Release

`hemtt release --no-bin --no-archive -v` completed successfully on Linux:

- 14 PBOs, 14 matching signatures, one public key.
- 1,647 SQF source files and 1,647 compiled SQFC entries.
- 14 packaged binary configs.
- All 18 changed/new runtime SQF sources match their packaged copies byte for byte.
- No missing changed source, mismatched source or Python cache files in the packages.
- `git diff --check` passed.

HEMTT used its bundled Arma wiki because the external wiki update was unavailable. Existing informational lint suggestions remain outside this change.

The Windows deployment script was reviewed but could not be executed in this Linux environment. It checks a clean tracked checkout, B158 identity, HEMTT completion and release signatures before copying to an existing mod directory outside the checkout. It does not delete local untracked signing files.

## Live check still required

No live Arma client or dedicated server ran here. Check the actual readable font/alignment, supported and manual Semi-Fowler reach/lift/release with Animate enabled, and paused middle-click CPR → BVM → CPR with the carrier continuously off. Exit the session completely and confirm that its carrier returns once. The uploaded screenshot and the supplied ACM, ACE and Animate files informed the investigation; no third-party animation system was globally disabled.
