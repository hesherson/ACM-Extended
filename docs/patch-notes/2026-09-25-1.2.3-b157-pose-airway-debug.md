# ACM Extended 1.2.3 / B157

Corrective patch on B156. Retains its supply sharing, medication compatibility, coagulation and earlier chest/IV fixes.

- First patient contact keeps the original weapon-preserving crouch. After an intervention, the medical menu may use ACM's generic continuous crouch for that patient. Entry and exit use interpolation. Closing the menu, changing patients and abandoned handoffs release their own pose without interrupting a newer procedure.
- Head tilt/chin lift preserves supported Semi-Fowler's. A medic already providing manual support is told to finish that maneuver before using the same hands for HTCL. Invalid callbacks do not reserve the airway. Old head-position sequence cleanup yields to a newer airway action.
- Check Breathing retains the provider's frozen carrier-handling pose throughout the native assessment timer. With a carrier, readiness is published at actual removal; without one, the check waits for its assessment hold. Failed preparation cancels instead of consuming the timer. Cancellation and completion release the corresponding pose and carrier custody.
- Carrier preparation uses matching generation tokens across the patient and provider owners so canceled messages cannot revive old preparation.
- The debug overlay uses one text size across its header, clinical and machine/network sections, shared label/value columns, aligned numeric fields and the original blue background. It remains one compact menu.
- Native treatments with an ACME animation-rate lease retain the shared choreography rate. Native short-action animation skips and clinical timer durations are preserved.

Build with `hemtt release` on Windows. Deploy the complete release output to clients and server, including all 14 PBOs and matching signatures. Install the matching server key and keep the separate original ACM disabled. Restart and confirm **1.2.3 / B157**.

Automated source and SQF-runtime checks do not render Arma animations or emulate the actual dedicated-server modpack. Verify first contact, treatment/cancel/menu exit, Semi-Fowler's to HTCL, carrier/no-carrier Check Breathing and another client's view of the frozen hold in game.
